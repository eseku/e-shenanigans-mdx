import styled from 'styled-components'

export default styled.p`
  text-align: center;
  font-family: raleway;
  font-size: 18px;
  margin-top: 5rem;
  color: ${props => props.theme.colors.textColor};
`
