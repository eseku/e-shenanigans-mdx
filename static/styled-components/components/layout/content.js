import styled from 'styled-components'
import { rhythm } from '../../../../src/utils/typography'

export default styled.div`
  flex-grow: 1;
  max-width: ${rhythm(50)};
  margin: 0 auto;
  padding: ${rhythm(0.2)} ${rhythm(0.75)};
`
